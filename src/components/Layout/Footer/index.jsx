import React from 'react';
import Counter from '../Counter';

export function Footer() {
  return (
    <footer>
      <Counter label={'footer'} />
      <Counter label={'footer'} />
      <Counter label={'footer'} />
      <Counter label={'footer'} />
    </footer>
  );
}
